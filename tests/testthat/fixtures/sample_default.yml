# Fixture: sample_default.yml
data:
  filename: "test_data.csv"
  target_variable: "TestTarget"
  features_to_drop:
    - "ID"
    - "Timestamp"
model:
  seed: 3
  cv_folds: 7
paths:
  artifacts: "test_artifacts" 